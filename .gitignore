# Jekyll build output and caches
_site/
.sass-cache
.jekyll-cache
.jekyll-metadata

# Bundler (local config + vendored gems)
.bundle/
vendor/

# Companion POCs repo (kept on-disk for local reference, not part of this site)
dBPF-pocs/

# OS / editor cruft
.DS_Store
*.bak
